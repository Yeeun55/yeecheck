# Yeecheck Design System

이 문서는 Yeecheck의 화면을 추가하거나 수정할 때 가장 먼저 확인하는 기준이에요.

## 핵심 원칙

- 조작을 설명하는 문구는 화면에 두지 않고 Lucide 아이콘과 접근성 이름을 사용해요.
- 사용자가 입력한 블록 내용, 날짜, 시간, 태그 이름은 데이터이므로 화면에 표시해요.
- 기본 UI는 흰색부터 검정까지 무채색만 사용하고, 사용자가 구분하는 태그 점에만 지정된 10가지 색을 사용해요.
- 글꼴은 Pretendard를 사용해요.
- 장식보다 정보 밀도, 정렬, 여백의 리듬을 우선해요.
- 큰 모달보다 화면 안 직접 입력과 작은 팝오버를 우선해요.

## 화면 구조

- 헤더 1행: D / W / 2W / M, 날짜 이동, 보관함 아이콘
- 헤더 2행: 태그 필터 가로 목록과 태그 편집 아이콘
- 본문: 날짜 열마다 일정 → 할일 → 타임테이블 → 메모 순서
- 보관함: 날짜가 없는 블록을 타입별로 보관하고 날짜 열로 드래그할 수 있어요.

## 날짜와 구획

- 날짜 머리에는 요일 한 글자와 일자만 표시해요.
- 각 타입 구획은 시작 아이콘만 사용하고 아이콘 오른쪽 선은 두지 않아요.
- 타입 구획 사이의 가는 가로선으로만 구조를 구분해요.

## 블록

- 블록을 한 번 클릭하면 옅은 회색 배경만 있는 선택 상태가 되고, 선택 테두리는 사용하지 않아요.
- 선택된 블록을 다시 클릭하거나 Enter를 누르면 제목 편집 상태가 돼요.
- 선택 상태에서 Esc는 선택 해제, Delete는 삭제로 동작해요.
- 제목 편집 중 Esc는 변경을 취소하고 해당 블록 선택 상태로 돌아가요.
- 제목 편집 중 Enter는 내용을 저장하고 해당 블록 선택 상태로 돌아가며, 내용이 비어 있으면 블록을 삭제해요.
- 선택 또는 제목 편집 상태에서 위·아래 화살표로 인접 블록 선택 상태를 이동해요.
- 같은 타입의 마지막 블록 다음에는 키보드로 선택할 수 있는 점선 빈 박스가 있고, Enter를 누르면 새 블록을 만들어 바로 편집해요.
- 할일만 제목 앞에 체크박스를 표시해요.
- 다른 블록은 제목 앞 타입 아이콘을 표시하지 않아요.
- 드래그 손잡이, 속성, 삭제 아이콘은 선택 여부와 관계없이 블록에 마우스를 올릴 때 보여요.
- 블록 속성은 전체 화면 모달이 아니라 블록 바로 아래 작은 팝오버에서 수정해요.
- 태그는 이름 없이 블록 제목 아래 작은 색상 점으로만 표시해요.
- 반복 할일은 제목 오른쪽에 반복 아이콘을 표시해요.
- 일정 기간은 블록 본문에 반복 표시하지 않고 속성 팝오버에서만 수정해요.
- 블록 사이의 빈 공간은 마우스를 올리면 점선 추가 영역이 나타나고 클릭하면 그 위치에 블록을 만들어요.
- 여러 날짜에 걸친 일정도 속성 팝오버는 클릭한 한 위치에만 열어요.
- 체크리스트는 다른 체크리스트 위로 드래그하면 자식이 되고, 블록 사이 추가 영역으로 빼면 연결이 끊겨요.
- 타임테이블은 시작 시간순으로 정렬하며 시간을 `09:00 ~ 11:30` 한 줄로 표시해요.
- 타임테이블 시간은 직접 입력하거나 위·아래 화살표로 시는 1시간, 분은 10분씩 조절해요.
- 배경, 아이콘, 점선 추가 영역, 팝오버, 사이드바 상태 변화에는 짧고 부드러운 전환을 사용해요.

## 태그

- 평상시 태그 칩은 블록 필터로 동작해요.
- 태그 아이콘 옆 눈 아이콘은 모든 블록의 태그 점을 한 번에 표시하거나 숨겨요.
- 태그 목록 오른쪽 편집 아이콘을 누르면 추가, 취소, 완료 아이콘이 나타나요.
- 편집 중 태그 칩을 누르면 이름, 10가지 태그 색상, 블록 위 표시 여부를 수정할 수 있어요.
- 태그 삭제 시 모든 블록의 해당 태그 연결도 함께 제거해요.
- 취소는 편집 시작 전 태그와 블록 연결 상태를 복원해요.

## 반응형과 접근성

- W / 2W / M은 좁은 화면에서 가로 스크롤을 허용해 날짜 열 너비를 보호해요.
- 모든 아이콘 버튼에는 보이는 문구 대신 `aria-label`을 제공해요.
- 포커스 윤곽선을 유지하고 모션 감소 설정을 존중해요.
